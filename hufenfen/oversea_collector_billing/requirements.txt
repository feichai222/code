# This file is used by pip to install required python packages
# Usage: python3 -m pip install -r requirements.txt -i https://pypi.doubanio.com/simple

# We use these packages as settings
numpy==1.22.1
pandas==1.3.5
xlwt==1.3.0
